# READ ME
-----------------------------
### 一、数据预处理
* `data/train` 目录下的内容为数据预处理程序
* `step1.py` 去除间隔为1和2的空缺
* `step2.py` 去除间隔大于2的空缺
-------------------------------------
### 二、曲线分类及划分
* `dtw_test.py` 通过dtw分类的脚本
* `fft.py` 通过fft分解原有图像
* `kmeans.py` 通过kmeans分类的脚本
* `stl_test` 通过stl分解原有图像